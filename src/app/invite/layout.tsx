/**
 * Invite Layout
 * 
 * Standalone layout for invite pages - no sidebar, full screen.
 * This overrides the default app layout to provide a clean invite experience.
 */
export default function InviteLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}






